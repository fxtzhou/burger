<li>
    {{#if devoured}}
    {{burger_name}}
    {{else}}
    {{burger_name}}
    <form action="/api/burgers/{{id}}?_method=POST" method="POST">
        <button class="btn btn-primary" data-id="{{id}}" type="submit">
            {{#if devoured}}ALREADY EATEN{{else}}CONSUME {{/if}}
        </button>
    </form>
    {{/if}}

</li>